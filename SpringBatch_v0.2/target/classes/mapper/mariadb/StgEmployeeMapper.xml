<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.spbatch.mapper.mariadb.StgEmployeeMapper">


    <select id="selectTestOne" resultType="String">
    	select 'TestMariaDB'  
    </select>

	<insert id="insertStgMariaDB">
		<![CDATA[
		    INSERT INTO stg_employees
		    (
		          employee_id
		        , first_name
		        , last_name
		        , email
		        , job_id
		        , salary
		        , hire_date
		        , src_dt
		    )
		    VALUES
		    (
		          #{EMPLOYEE_ID}
		        , #{FIRST_NAME}
		        , #{LAST_NAME}
		        , #{EMAIL}
		        , #{JOB_ID}
		        , #{SALARY}
		        , #{HIRE_DATE}
		        , NOW()
		    )
		]]>
	</insert>
    
    
</mapper>